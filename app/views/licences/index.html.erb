<% if current_user.admin? %>
  <h1>Manage Licences</h1>
  <hr>
  <%= render "owner_index", licences: @licences %>
<% else %>
  <h1>My Licences</h1>
  <hr>
  <%= render "applicant_index", licences: @licences %>
<% end %>
