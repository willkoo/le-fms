<h1>Franchises#index</h1>
<br>
<% if @franchises.any? %>
  <% @franchises.each do |franchise| %>
    <%= franchise.id %>.
    <%= franchise.name %>,<br>
    Founder: <%= franchise.user.profiles.first.first_name %><br>
    Description: <%= franchise.description %>,<br>
    <%= link_to "View details", franchise_path(franchise) %><br><br>
  <% end %>
<% else %>
  <p>No franchises yet.</p>
<% end %>
<br>

<%= link_to "Create", new_franchise_path(@franchise) %>
