<%= simple_form_for [licence ,quiz] do |f| %>
  <%= f.input :video_completed,:as => :hidden, :input_html => { :value => true } %>
  <%= f.submit :"I've completed the video", :class => "btn btn-primary w-100" %>
<% end %>
